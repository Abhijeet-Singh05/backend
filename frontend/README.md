# Frontend scaffold

This directory contains a Vite + React + TypeScript + Tailwind starter for the social app frontend.

Location on this machine: `E:\mscode\web\backend\frontend`

Quick start:

```bash
cd frontend
npm install
npm run dev
```
